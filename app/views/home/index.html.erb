<ul>
  <% if current_user %>
  <%    current_user.apps.each{|app| %>
      <li><%=link_to app, app_path(app) %></li>
  <%    } %>
  <% else %>
  <%    App.limit(10).each{|app| %>
      <li><%=app.name %></li>
  <%    } %>
  <% end %>
</ul>
