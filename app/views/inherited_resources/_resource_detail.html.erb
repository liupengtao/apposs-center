<table class="<%= collection.first.class.to_s.downcase %> detail">
<% resource.attributes.each_pair do |attr, value| %>
  <% next if ['id', 'created_at', 'updated_at'].include?(attr) %>
  <tr>
    <th><%= resource.class.human_attribute_name attr.to_sym %></th>
    <td><%= value %></td>
  </tr>
<% end %>
</table>
